import LoginForm from "../components/LoginForm";

const page = async () => {
  return (
    <div className="flex justify-center items-center mt-16">
      <LoginForm />
    </div>
  );
};

export default page;
